# Database
DATABASE_URL=your_database_url_here

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# OAuth State Encryption
OAUTH_STATE_SECRET=your_oauth_state_secret_here
ENCRYPTION_KEY=your_encryption_key_for_oauth_secrets

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ====================================
# DYNAMIC OAUTH CONFIGURATION
# ====================================
# This is a multi-tenant SaaS application where each user
# configures their own OAuth app credentials
# 
# Users will:
# 1. Go to Settings → Integrations
# 2. Add their OAuth app credentials for each platform
# 3. Connect to platforms using their own OAuth apps
#
# No environment variables are needed for OAuth platforms.
# All OAuth credentials are stored per-user in the database.